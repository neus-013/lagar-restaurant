// Configuración de Astro
// (pendiente: aquí no hay nada de diseño ni contenido todavía, solo la config mínima para que el proyecto arranque)
import { defineConfig } from 'astro/config';

export default defineConfig({});
